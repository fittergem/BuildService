local Checks = {}

function Checks:InBounds(cframe: CFrame, plot: Model)
	
end

function Checks:Collision(object: Model, plotObjects: Folder)
	
end

function Checks:PositionValid(cframe: CFrame, plot: Model)
	
end

return Checks
